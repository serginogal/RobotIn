import styles from './styles.module.scss'

const Logo = () => {
  return (
    <span className={styles.logo}>
      Robot
      <span className={styles.logoBolder}>In</span>
    </span>
  )
}

export default Logo
